<%= form_for(@escalation_policy) do |f| %>
  <% if @escalation_policy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@escalation_policy.errors.count, "error") %> prohibited this escalation_policy from being saved:</h2>

      <ul>
      <% @escalation_policy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
