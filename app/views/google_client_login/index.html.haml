%h1 Google ClientLogin Tokens

%table
  %tr
    %th Email
    %th Last refreshed at
    %th Actions

  - @credentials.each do |credential|
    %tr
      %td= credential.email
      %td= credential.updated_at
      %td= link_to "Delete", credential, :method => :delete

%div= link_to "New ClientLogin token", new_google_client_login_credentials_path
