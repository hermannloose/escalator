<p id="notice"><%= notice %></p>

<h1>Details for <%= link_to @membership.user.name, @membership.user %> in
    <%= link_to @membership.rotation.name, @membership.rotation %></h1>

<h3><%= link_to "Alerting steps:", [@membership, :alerting_steps] %></h3>
<ol>
  <% @membership.alerting_steps.each do |step| %>
    <li>if I do not respond within <strong><%= step.delay_minutes %>
    minutes</strong>, contact me via
  <%= link_to step.contact_detail.name, [:profile, step.contact_detail] %>.
  <%= link_to "Delete", [@membership, step], :method => :delete %></li>
  <% end %>
</ol>

<%= link_to "New alerting step", new_rotation_membership_alerting_step_path(@membership) %>
